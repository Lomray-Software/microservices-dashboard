// import type { FC } from 'react';
// import React from 'react';
// import Field from '@components/forms/field';
// import type { ISelectOptions } from '@components/forms/select-field';
// import SelectField from '@components/forms/select-field';
//
// interface IEntityField {
//   name: string;
//   type?: 'select' | 'text' | 'date'; // text by default
//   options?: ISelectOptions;
// }
//
// export interface IFields {
//   fields: IEntityField;
// }
//
// const Fields: FC<IEntityField> = ({ fields }) =>
//   type === 'select' ? (
//     <SelectField options={options} name={name} title={name} isInline={isInline} />
//   ) : (
//     <Field type={type} name={name} placeholder={placeholder} title={name} isInline={isInline} />
//   );
//
// export default Fields;
